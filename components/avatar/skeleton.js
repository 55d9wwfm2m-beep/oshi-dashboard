/* ============================================================
   共通スケルトン座標（全パーツの作図基準）
   ------------------------------------------------------------
   すべてのパーツは viewBox "0 0 320 380" の同一座標系で作図する。
   髪型・服を差し替えても位置がズレないのは、全パーツが
   このアンカーに従って描かれているため。

   ▼ レイヤースタック（AvatarSVG.jsx はこの順で必ず合成する）
      1. background       背景
      2. groundShadow     接地影
      3. hairBack         後ろ髪
      4. legs             足（靴含む）
      5. arms             腕
      6. neck             首
      7. torso            胴（服の下地）
      8. outfit           服
      9. faceBase         顔土台（耳・輪郭・眉・鼻）
     10. cheeks           チーク
     11. eyes             目
     12. mouth            口
     13. hairFront        前髪
     14. glasses          メガネ
     15. headwear         髪飾り
     16. accessoryFront   前景アクセサリー

   ▼ 予約領域（衝突ルール）
   - 目領域 (x115〜205, y112〜152): 前髪の不透明塗りで完全に
     覆わないこと（覆うデザインは透け処理を入れる）。
   - 帽子領域 (y<84): ここに入る髪（お団子等）は帽子との
     併用時の見た目を必ず確認する。
   ============================================================ */

/** 頭の中心（髪・帽子・メガネの基準点） */
export const HEAD_CENTER = { x: 160, y: 112 };
/** 目の中心線Y／左右の目の中心X */
export const EY = 134;
export const ELX = 132;
export const ERX = 188;
/** 主要アンカー（新パーツはここに合わせて作図する） */
export const ANCHORS = {
  headTop: 44,      // 頭スキン上端
  chin: 180,        // あご下端
  shoulder: 194,    // 肩ライン
  waist: 240,       // ウエスト（ベルト・ロング髪の目安）
  hem: 296,         // スカート裾の下限
  handL: { x: 136, y: 246 },
  handR: { x: 184, y: 246 },
  sole: 333,        // 接地（靴底）
};

/** 顔の輪郭パス（しずく型） */
export const HEAD_PATH =
  "M160,44 C126,44 102,76 100,118 C98,150 122,180 160,180 C198,180 222,150 220,118 C218,76 194,44 160,44 Z";

/**
 * 旧ヘッド基準で作図された既存髪型をフィットさせる互換変換。
 * 【重要】新しい髪型はこの変換に依存せず、素の座標系で作図し
 * HAIR_R のエントリに fit: 'skeleton' を指定すること。
 */
export const HAIR_LEGACY_FIT = "translate(160 112) scale(1.09 1.08) translate(-160 -113)";

/** viewBox は全アイテム共通・固定（アイテムごとに変えない） */
export const VIEWBOX = "0 0 320 380";

/** 表示クロップのプリセット（ratio = 高さ/幅） */
export const CROPS = {
  full: { vb: "0 0 320 380", ratio: 380 / 320 },
  bust: { vb: "44 22 232 232", ratio: 1 },      // 顔・髪用サムネ
  torso: { vb: "56 170 208 208", ratio: 1 },    // 服用サムネ（あご下〜足元）
};
